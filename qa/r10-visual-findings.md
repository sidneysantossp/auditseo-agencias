# R10 Visual Findings — Desktop

A inspeção real de `qa/r10-screenshots/desktop-hero.png` e `qa/r10-screenshots/desktop-full.png` foi concluída em 1440px.

A hero apresenta o H1 completo, subtítulo, metadados, autoria e featured image 1200×630 sem texto. O artigo utiliza o dark charcoal/gold do Design System V3, com contraste adequado e largura de leitura controlada. A primeira dobra mostra o início do long-form e o TOC lateral.

A captura full confirma coluna principal, sidebar com TOC ativo, bloco de autoria e compartilhamento no mesmo fluxo vertical. O sidebar não apresenta scroll interno; o indicador de progresso permanece no extremo direito. Os frameworks editoriais aparecem como cards e diagramas monoespaçados, sem overflow visível. Não foi observado bug visual real no desktop.

# R10 Visual Findings — Mobile

A inspeção real de `qa/r10-screenshots/mobile-hero.png` e `qa/r10-screenshots/mobile-full.png` foi concluída em 390px.

A navbar colapsa para logo e menu hamburger. O H1 longo quebra em linhas legíveis, sem overflow horizontal; subtítulo, autoria e imagem seguem o fluxo vertical. A featured image mantém a proporção e o enquadramento. O índice móvel aparece como painel colapsável antes do artigo, e o primeiro framework/definition box permanece dentro da largura de leitura. Não foi observado bug visual real no mobile.
